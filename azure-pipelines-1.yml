trigger: none

pr:
  branches:
    include:
      - '*'

pool:
  name: Default

steps:
- task: AICodeReview@1
  inputs:
    apiKey: $(AI_API_KEY)
    apiEndpoint: 'https://api.groq.com/openai/v1'
    modelName: 'llama-3.3-70b-versatile'
  displayName: 'AI Code Review'